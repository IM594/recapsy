export const DEFAULT_DB_URL = "ws://127.0.0.1:21890" as const;
export const DEFAULT_DB_NAMESPACE = "recaply" as const;
export const DEFAULT_DB_DATABASE = "sense" as const;
export const DEFAULT_DB_USERNAME = "root" as const;
export const DEFAULT_DB_PASSWORD = "root" as const;

export const DEFAULT_BASE_DIR = "~/Library/Application Support/RecaplySense" as const;
export const DEFAULT_SCREENSHOTS_DIR = "screenshots" as const;
export const DEFAULT_THUMBNAILS_DIR = "thumbnails" as const;
export const DEFAULT_LOGS_DIR = "logs" as const;
export const DEFAULT_DB_DIR = "db" as const;

export const DEFAULT_EMBEDDING_MODEL = "bge-m3-v1" as const;
export const EMBEDDING_DIMENSIONS = 1024 as const;
