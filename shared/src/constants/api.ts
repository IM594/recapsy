export const API_MOUNTS = {
  api: "/api",
  summary: "/api/summary",
} as const;

export const API_ROUTES = {
  repos: "/repos",
} as const;

export const SUMMARY_ROUTES = {
  years: "/years",
  generate: "/generate",
  events: "/events",
  status: "/status",
  reset: "/reset",
  data: "/data",
  regenerate: "/regenerate",
} as const;

