# Recaply Sense 里程碑与验收（MILESTONES）

> 版本：v0.1（文档阶段）
> 状态：待确认
> 关联规格：`/Users/user/Downloads/projects/my/recaply-sense/SPEC_v2.md`

## 1. 里程碑总览

| 里程碑 | 目标周期 | 核心目标 |
| --- | --- | --- |
| M0 | 2 周 | 离线闭环 + 可双击运行 `.app` |
| M1 | 2-3 周 | 音频 + 混合检索 + Provider 框架 |
| M1.1 | 1-2 周 | Ask + Chat 可用，答案必须带引用 |

## 2. 统一门禁（全部里程碑共享）

1. 必须满足 Offline 默认可用。
2. Cloud 能力必须是可插拔增强，不得成为硬依赖。
3. 任一 Cloud 失败必须自动回退 Offline，且用户主流程不中断。
4. 每项功能至少 1 个测试，关键链路必须纳入回归集。
5. 发布产物必须是可双击运行的 `.app`（对应阶段最小能力）。

## 3. M0：离线闭环

### 3.1 范围

1. 菜单栏 App：启动/停止/暂停/恢复。
2. 屏幕采集 -> 去重 -> OCR -> 入库 -> FTS 搜索闭环。
3. MCP `recapsense_search`、`recapsense_get_chunk`。
4. 可双击运行 `.app`。

### 3.2 里程碑交付物

1. 可运行 App（开发签名）。
2. SQLite schema v1（含 FTS 和关键索引）。
3. M0 回归测试集（最小可持续）。
4. 打包脚本与运行说明。

### 3.3 可执行验收标准

1. 功能验收
- 启动应用并授权屏幕录制后，连续采集 10 分钟。
- 执行关键词搜索，命中结果可回看对应 chunk。
- MCP `search/get_chunk` 返回结构化结果。

2. 稳定性验收
- 长跑 8 小时期间进程无崩溃。
- 强制退出后重启，数据仍完整可查。

3. 自动化验收
- `xcodebuild -scheme RecaplySense build` 成功。
- `xcodebuild -scheme RecaplySense test` 成功。
- 回归用例至少覆盖：单实例锁、权限探测、OCR 入库、FTS 检索、MCP 基础工具。

4. 质量门槛
- M0 新增核心模块测试通过率 100%。
- 无 P0/P1 未解决缺陷。

## 4. M1：音频与混合检索

### 4.1 范围

1. 音频采集：仅麦克风激活时采集。
2. ASR：本地优先，必要时可配置云增强。
3. 混合检索：FTS + 向量 + 融合排序。
4. Provider 框架：Embedding/Vector/ObjectStorage 最少落地 local + 1 个可选 cloud 实现。
5. SyncCore：outbox + 幂等上传 + 指数退避。
6. MCP `Streamable HTTP` + token 鉴权。

### 4.2 里程碑交付物

1. 音频链路完整可用并可配置保留期。
2. 混合检索 API 与排序策略实现。
3. ProviderSDK 与最小 cloud provider 实现。
4. SyncCore worker 与冲突日志机制。

### 4.3 可执行验收标准

1. 功能验收
- 麦克风未激活时不产生音频记录。
- 麦克风激活后可生成 `audio_segments` 并被检索。
- 混合检索结果在语义相近问题下优于纯 FTS（基准集评估）。

2. 回退验收（关键）
- 人为模拟 cloud provider 网络失败、鉴权失败、超时。
- 断言请求自动回退本地 provider 并返回成功结果。
- 断言本地写入与查询路径不受阻塞。

3. 同步验收
- 本地写入后必有 `sync_outbox` 事件。
- 上传失败按指数退避重试。
- 重放同一 `idempotency_key` 不产生重复数据。

4. 自动化验收
- `xcodebuild -scheme RecaplySense test` 成功。
- 集成测试覆盖：音频 gating、ASR 入库、混合检索、fallback、sync outbox。

## 5. M1.1：Ask + Chat（带引用）

### 5.1 范围

1. 实现 `ask(question, filters)`。
2. Chat UI 从占位升级为可用。
3. MCP `recapsense_ask` 上线。
4. 输出 `answer + citations[]`，支持引用跳转。

### 5.2 里程碑交付物

1. AskService（本地检索主路径 + 可选云 LLM）。
2. Chat UI 与引用展示组件。
3. 20 题标准回归样本集与评分脚本。

### 5.3 可执行验收标准

1. 引用完整性验收
- 任意问答输出必须包含 `citations[]`。
- 每条 citation 均可定位到 `chunk` 或 `audio_segment`。

2. 可定位率验收（硬门槛）
- 在 20 个标准问题样本上执行回归。
- 引用可定位率必须达到 100%。

3. 回退验收
- 云 LLM 不可用时自动降级为“本地检索 + 本地答案模板”。
- 回退后 API 字段结构不变。

4. 自动化验收
- `xcodebuild -scheme RecaplySense test` 成功。
- Ask 与 Chat 关键链路回归用例全部通过。

## 6. 风险、触发器与应对

| 风险 | 触发器 | 应对策略 | 验证方式 |
| --- | --- | --- | --- |
| 本地向量引擎效果不稳定 | 语义检索误召回率高 | 保留 L1 最小能力，快速切换云向量增强 | 混合检索基准回归 |
| 音频识别设备差异 | 不同机型识别准确率波动 | 本地优先 + 云可插拔 + 用户策略可配置 | 设备矩阵测试 |
| 媒体占满磁盘 | 媒体目录增长异常 | 热窗口清理 + 阈值告警 + 可选云归档 | 存储水位监控 |
| MCP 协议演进风险 | 客户端版本不一致 | 固定协议版本 + 兼容性测试集 | 协议回归测试 |

## 7. SPEC 映射

| SPEC 章节 | 对应里程碑条目 |
| --- | --- |
| 5 双模式运行 | 统一门禁 1-3、M1 回退验收、M1.1 回退验收 |
| 7 向量策略 | M1 范围 3、M1 验收 1/2 |
| 8 音频策略 | M1 范围 1/2、M1 验收 1 |
| 11 云能力与同步 | M1 范围 4/5、M1 验收 2/3 |
| 12 MCP | M0 范围 3、M1 范围 6、M1.1 范围 3 |
| 13 聊天引用 | M1.1 范围 1/2/4、M1.1 验收 1/2 |
| 17 里程碑验收 | 全文 |
